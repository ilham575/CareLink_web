# cloudbuild.yaml สำหรับ Web Client
steps:
  # Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/$_IMAGE_NAME:latest'
      - '--build-arg'
      - 'REACT_APP_API_URL=${_API_URL}'
      - '--build-arg'
      - 'REACT_APP_SOCKET_URL=${_SOCKET_URL}'
      - '--build-arg'
      - 'REACT_APP_TELEGRAM_BOT_NAME=${_TELEGRAM_BOT_NAME}'
      - '.' # รันจากตำแหน่งปัจจุบันที่ส่งไฟล์ขึ้นไป

  # Step 2: Push image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-image'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/$_IMAGE_NAME:latest'

  # Step 3: Deploy to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    id: 'deploy-cloud-run'
    args:
      - 'run'
      - 'deploy'
      - '$_SERVICE_NAME'
      - '--image'
      - 'gcr.io/$PROJECT_ID/$_IMAGE_NAME:latest'
      - '--region'
      - '$_REGION'
      - '--platform'
      - 'managed'
      - '--port'
      - '8080'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'REACT_APP_API_URL=$_API_URL,REACT_APP_SOCKET_URL=${_SOCKET_URL},REACT_APP_TELEGRAM_BOT_NAME=$_TELEGRAM_BOT_NAME'
    waitFor: ['push-image']

substitutions:
  _IMAGE_NAME: 'web-client'
  _SERVICE_NAME: 'carelink-web-client'
  _REGION: 'asia-southeast1'
  _API_URL: 'https://carelink-strapi-ke4rorabaa-as.a.run.app'
  _SOCKET_URL: 'wss://carelink-strapi-290491493302.asia-southeast1.run.app'
  _TELEGRAM_BOT_NAME: 'carelink2_bot'

options:
  logging: CLOUD_LOGGING_ONLY