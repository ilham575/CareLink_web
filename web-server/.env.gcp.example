# Environment variables สำหรับ GCP deployment
# Copy ไฟล์นี้เป็น .env.gcp แล้วแก้ไขค่า

# Node Environment
NODE_ENV=production

# Database Configuration
DATABASE_CLIENT=postgres
DATABASE_HOST=cloudsql-proxy  # หรือ IP ของ Cloud SQL Proxy
DATABASE_PORT=5432
DATABASE_NAME=carelink_db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your-strong-password-here
DATABASE_SSL=false
DATABASE_LOGGING=false

# Strapi Security Keys (ต้องเปลี่ยนค่าเป็นค่า random ที่แข็งแรง)
# สร้างด้วย: node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"
ADMIN_JWT_SECRET=your-admin-jwt-secret-key-change-this
JWT_SECRET=your-jwt-secret-key-change-this
API_TOKEN_SALT=your-api-token-salt-change-this
TRANSFER_TOKEN_SALT=your-transfer-token-salt-change-this

# Server Configuration
HOST=0.0.0.0
PORT=1337
URL=https://your-domain.com  # Update with your GCP domain

# Strapi Admin Configuration
STRAPI_ADMIN_BACKEND_URL=https://your-domain.com
STRAPI_URL=https://your-domain.com

# Optional: Google Cloud Storage for uploads
# PLUGIN_UPLOAD_PROVIDER=google-cloud-storage
# PLUGIN_UPLOAD_GCS_BUCKET=your-gcp-bucket-name
# PLUGIN_UPLOAD_GCS_PROJECT_ID=your-gcp-project-id
# PLUGIN_UPLOAD_GCS_KEY_FILE=/app/gcp-key.json

# Logging
LOG_LEVEL=info
