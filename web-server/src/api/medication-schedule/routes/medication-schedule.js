'use strict';

/**
 * medication-schedule router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::medication-schedule.medication-schedule');
