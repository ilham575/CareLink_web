'use strict';

/**
 * medication-schedule controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::medication-schedule.medication-schedule');
